/target
/target-cuda
models/
escrow.key
escrow_state.json
.env

# Prebuilt release binaries live at repo root during local builds/testing; uploaded as GitHub
# release assets, not committed to git history.
/keryx-miner-linux-x64*
/keryx-miner-windows-x64*.exe

target-hive/
Cargo.lock

# Android app: build outputs, machine-local SDK path, and the cargo-ndk-built .so (rebuilt via
# `./gradlew :app:cargoNdkBuild`, not committed).
android-app/.gradle/
android-app/local.properties
android-app/app/build/
android-app/build/
android-app/app/src/main/jniLibs/
